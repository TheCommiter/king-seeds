# Networking/firmware helpers (pick what you want)
b43-fwcutter
bcmwl-kernel-source

# NVIDIA meta (match noble’s HWE versions)
(/^nvidia-driver-550$/)
(/^linux-modules-nvidia-550-generic-hwe-24.04$/)
nvidia-prime

# Installer bootloaders (both BIOS & UEFI)
grub-efi-amd64-signed [amd64]
shim-signed [amd64]
grub-pc [amd64]

# Blacklist duplicates
!grub-efi-amd64
